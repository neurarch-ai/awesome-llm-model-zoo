{
  "name": "neurarch-model-zoo-scripts",
  "private": true,
  "type": "module",
  "description": "Maintainer tooling for regenerating zoo entries. Requires the Neurarch source checkout as a sibling directory (../Neurarch).",
  "scripts": {
    "generate": "tsx --tsconfig ../Neurarch/tsconfig.json scripts/generate.mts",
    "validate": "node scripts/validate.mjs",
    "catalog": "node scripts/generate-catalog.mjs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "pngjs": "^7.0.0",
    "tsx": "^4.19.0"
  }
}
